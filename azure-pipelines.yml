# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------
variables:
    - group: secrets

stages:
  - stage: windows_latest
    displayName: 'windows-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'windows-latest'
    jobs:
      - job: PackNuget
        displayName: 'PackNuget'
        dependsOn: [  ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd DownloadAntlrTool Restore BuildSolution PackNuget --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: nupkg
              pathtoPublish: '.artifacts/nupkg'
      - job: InstallTool
        displayName: 'InstallTool'
        dependsOn: [ PackNuget ]
        steps:
          - task:  DownloadBuildArtifacts@0
            displayName: Download Nuget artifacts
            inputs:
              artifactName: 'nupkg'
              downloadPath: $(Build.SourcesDirectory)/.artifacts
          - task: CmdLine@2
            inputs:
              script: './build.cmd InstallTool --skip'
      - job: PublishNuget
        displayName: 'PublishNuget'
        dependsOn: [ InstallTool ]
        steps:
          - task: DownloadBuildArtifacts@0
            displayName: Download Nuget artifacts
            inputs:
              artifactName: 'nupkg'
              downloadPath: $(Build.SourcesDirectory)/.artifacts
          - task: CmdLine@2
            inputs:
              script: './build.cmd PublishNuget --nuget-api-key $(NugetApiKey) --skip'
